<!DOCTYPE html>
<html>
<head>
  <title>DevNotes</title>
  <meta name="viewport" content="width=device-width; initial-scale=1;" />
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="container">
    <% flash.each do |key, value| %>
      <div class="center auto_close fade_in <%= flash_class(key) %>">
        <button class="close" data-dismiss="alert">×</button>
        <%= value %>
      </div>
    <% end %>

    <div id="account-widget">
      <div id="account-info">
      <% if current_account %>
        Welcome <strong><%= current_account.name %></strong>!
        <%= link_to "Sign out", sign_out_path %>
      <% else %>
        <%= link_to "Sign in with Facebook", "/auth/facebook", id: "sign_in" %>
      <% end %>
      </div>
    </div>

    <div class="row">
      <div class="col-md-6 col-centered">
        <div id="custom-search-input">
          <div class="input-group col-md-12">
            <form action="/search" accept-charset="UTF-8" method="get">
              <input name="utf8" type="hidden" value="&#x2713;" />
              <%= text_field_tag :q, params[:q], class: 'form-control input-lg', placeholder: 'Search for notes here' %>
              <span class="input-group-btn">
                <button class="btn btn-info btn-lg" type="button" name="">
                  <i class="fa fa-search"></i>
                </button>
              </span>
            </form>
          </div>
        </div>
      </div>
    </div>

    <%= yield %>

  </div>

</body>
</html>
